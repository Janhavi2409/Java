/*
 * ->wrapping of the data in single unit
 * eg: capsule
 * ->private values are accessible only in that class sor to access them outside the class, java has
 * introduced getters and setters
 * ->we can make the data read only
 * ->we can control data, and we can control data hiding
 * */