/*
* ->value defined in a final keyword will always be the same and we will not be able to change it
* ->we can initialize blank final variable in constructor
* ->if there is static final variable then in that case it will be call as static blank final variable and we cannot change the value of that variable
* eg String PAN-NO = "ABC123458";
* generally final value is initialized in capital letters
*
*
* ->if our VARIABLE is final then we will not be able to CHANGE the final value
* ->if  our METHOD is final then we cannot OVER RIDE the final method
* ->if our CLASS is final then we cannot EXTEND the final class*/
